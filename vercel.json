{
  "version": 2,
  "builds": [
    {
      "src": "first_asp_app.csproj",
      "use": "@vercel/static-build",
      "config": {
        "command": "/usr/local/share/dotnet/dotnet publish -c Release -o publish",
        "output": "publish"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/publish/$1"
    }
  ],
  "env": {
    "ASPNETCORE_ENVIRONMENT": "Production"
  }
}

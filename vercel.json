{
  "version": 2,
  "buildCommand": "dotnet publish -c Release -o .vercel/output/static",
  "outputDirectory": ".vercel/output/static",
  "builds": [
    {
      "src": "**/*.cs",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/$1"
    }
  ]
 }
